services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mudita
      POSTGRES_PASSWORD: mudita
      POSTGRES_DB: mudita
    ports:
      - "5432:5432"
    volumes:
      - mudita_pg:/var/lib/postgresql/data

volumes:
  mudita_pg:
